import { createSlice } from '@reduxjs/toolkit'

const initialState = {
    isAuthenticated = true
}


const AuthSlice = createSlice({
    name: 'Auth',
    initialState,
    reducers : {
        
    }
})